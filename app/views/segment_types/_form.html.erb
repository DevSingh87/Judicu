<%= form_for(@segment_type) do |f| %>
  <% if @segment_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@segment_type.errors.count, "error") %> prohibited this segment_type from being saved:</h2>

      <ul>
      <% @segment_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_name %><br />
    <%= f.text_field :type_name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <%= f.fields_for :segment_names do |bundler| %>
  <%= render 'segment_name_fields', f: bundler %>
	  
  <% end %>
  <%= link_to_add_fields "Add Answer", f, :segment_names %> 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
